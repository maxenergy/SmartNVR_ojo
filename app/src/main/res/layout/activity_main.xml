<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_surveillance"
        android:name="com.wulala.myyolov5rtspthreadpool.ui.SurveillanceFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:layout="@layout/fragment_surveillance" />

    <!-- AI分析切换按钮 -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_ai_toggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="88dp"
        android:src="@drawable/ic_eye"
        app:srcCompat="@drawable/ic_eye"
        app:tint="@color/white"
        android:backgroundTint="@color/purple_500"
        android:contentDescription="切换AI分析"/>

    <!-- 设置按钮 -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_settings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:src="@drawable/ic_settings"
        app:srcCompat="@drawable/ic_settings"
        app:tint="@color/white"
        android:backgroundTint="@color/fab_background"
        android:contentDescription="设置"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>